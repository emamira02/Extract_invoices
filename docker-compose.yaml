services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - path: .env
        required: false
    volumes:
      - history:/app/data
      # Uncomment to enable Microsoft login:
      # - ./.streamlit/secrets.toml:/app/.streamlit/secrets.toml:ro

volumes:
  history:
